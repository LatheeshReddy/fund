package com.sts.springbootfundtransfer.util;

import java.time.LocalDate;

import com.sts.springbootfundtransfer.entity.Account;

public class AccountProcessor {

	/*
	 * public Account getAccount(Account account) {
	 * 
	 * 
	 * Account acc=new Account(); //acc.getBankId();
	 * //acc.setAccountNo(ACCOUNTNO++); acc.setAccountTypeId(1);//[1=saving
	 * 2=current] acc.setActive("1");//0=inactive 1=active
	 * acc.setBalance(account.getBalance()); acc.setDate(LocalDate.now());
	 * acc.setBankId(account.getBankId()); return accountRepository.save(acc);
	 * 
	 * }
	 */
}
